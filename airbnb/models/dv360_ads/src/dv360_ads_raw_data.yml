version: 2

sources:
  - name: marketing_source
    database: AIRBNB
    schema: "{{ var('schema_source') }}"
    tables:
      - name: DV360_RAW_DATA
        description: "Raw data table from Display Video 360 Ads API."
  