version: 2

sources:
  - name: marketing_source
    database: AIRBNB
    schema: "{{ var('schema_source') }}"
    tables:
      - name: GOOGLE_ADS_RAW_DATA
        description: "Raw data table from Google Ads API."
