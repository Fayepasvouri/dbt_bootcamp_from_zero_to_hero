version: 2
 
sources: 
  - name: "{{ var('schema_source') }}"
    tables:
      - name: GOOGLE_ADS_RAW_DATA
        description: "This table contains the raw data from Google Ads. It includes all the fields as they are received from the Google Ads API, without any transformations or cleaning applied. This is the source of truth for all Google Ads data in our analytics environment."